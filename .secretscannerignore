# One glob pattern per line, matched against paths relative to the scan root.
# Supports *, ** and ? (a reduced subset, not the full gitignore spec).
# Lines starting with # are comments.
#
# Examples:
# **/*.snap

# This project's own test fixtures intentionally contain fake secrets.
test/fixtures/**
test/*.test.ts

# Dependency lockfile hashes trigger high-entropy false positives.
package-lock.json
